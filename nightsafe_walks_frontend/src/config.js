  // Central configuration for NightSafe Walks frontend

  // PUBLIC_INTERFACE
  const config = {
    weatherApiKey: '285568c9e57ca0da76e2fe74bb901764',
    weatherUnits: 'metric', // or 'imperial'
    // TODO: In a production system, load from environment or process.env.REACT_APP_OPENWEATHERMAP_API_KEY
  };

  export default config;
